spring.application.name=projectsourcecode
#spring.mvc.view.prefix=/WEB-INF/jsp/
#spring.mvc.view.suffix=.jsp

# MySQL Database Config
spring.datasource.url=jdbc:mysql://localhost:3306/projectsourcecode?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=bagga

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

server.port=8081

# JWT config — make this a long random string in production and store in ENV
jwt.secret=replace_this_with_a_very_long_random_secret_at_least_32_chars_min
jwt.expiration-ms=3600000  # 1 hour in milliseconds
